"use client";
import { Toaster as ToasterComponent, toast } from "sonner";

export const Toaster = () => {
  return <ToasterComponent position="top-center" />;
};
//el toaster necesita acceso al cliente
